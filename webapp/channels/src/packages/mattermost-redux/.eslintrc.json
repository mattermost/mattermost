{
  "rules": {
    "no-restricted-imports": ["error", {
      "patterns": [
        {"group": ["actions/*", "selectors/*", "types/*", "utils/*", "!utils/test_helper"], "message": "mattermost-redux can't import files from the rest of the web app"}
      ]
    }]
  }
}
