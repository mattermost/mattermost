version: '2.4'
services:
  mysql:
    image: "mysql:5.6"
    restart: always
    environment:
      MYSQL_ROOT_HOST: "%"
      MYSQL_ROOT_PASSWORD: mostest
      MYSQL_PASSWORD: mostest
      MYSQL_USER: mmuser
      MYSQL_DATABASE: mattermost_test
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      interval: 5s
      timeout: 10s
      retries: 3
    tmpfs: /var/lib/mysql
    volumes:
      - "./docker/mysql.conf.d:/etc/mysql/conf.d"
    networks:
      default:
        aliases:
          - mysql
#  mysql-replica-ro:
#    image: "mysql:5.7"
#    restart: always
#    ports:
#      - 3307:3306
#    environment:
#      MYSQL_ROOT_HOST: "%"
#      MYSQL_ROOT_PASSWORD: mostest
#      MYSQL_PASSWORD: mostest
#      MYSQL_USER: mmuser
#      MYSQL_DATABASE: mattermost_test
#    healthcheck:
#      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
#      interval: 5s
#      timeout: 10s
#      retries: 3
#    volumes:
#      - "./docker/mysql.conf.d:/etc/mysql/conf.d"
#    networks:
#      default:
#        aliases:
#          - mysql-replica-ro
  # START SAME AS POSTGRES
  minio:
    extends:
      file: gitlab-dc.common.yml
      service: minio
    networks:
      default:
        aliases:
          - minio
  inbucket:
    extends:
      file: gitlab-dc.common.yml
      service: inbucket
    networks:
      default:
        aliases:
          - inbucket
  openldap:
    extends:
      file: gitlab-dc.common.yml
      service: openldap
    networks:
      default:
        aliases:
          - openldap
  elasticsearch:
    extends:
      file: gitlab-dc.common.yml
      service: elasticsearch
    networks:
      default:
        aliases:
          - elasticsearch
  dejavu:
    extends:
      file: gitlab-dc.common.yml
      service: dejavu
    networks:
      default:
        aliases:
          - dejavu
  keycloak:
    extends:
      file: gitlab-dc.common.yml
      service: keycloak
    networks:
      default:
        aliases:
          - keycloak
  prometheus:
    extends:
      file: gitlab-dc.common.yml
      service: prometheus
    networks:
      default:
        aliases:
          - prometheus
  grafana:
    extends:
      file: gitlab-dc.common.yml
      service: grafana
    networks:
      default:
        aliases:
          - grafana
  # END SAME AS POSTGRES

  start_dependencies:
    image: mattermost/mattermost-wait-for-dep:latest
    depends_on:
      - mysql
#      - mysql-replica-ro
      - minio
      - inbucket
      - openldap
      - elasticsearch
      - prometheus
      - grafana
#    command: mysql:3306 mysql-replica-ro:3007 minio:9000 inbucket:10080 openldap:389 elasticsearch:9200 prometheus:9090 grafana:3000
    command: mysql:3306 minio:9000 inbucket:10080 openldap:389 elasticsearch:9200 prometheus:9090 grafana:3000
    networks:
      default:

networks:
  default:
    external:
      name: ${COMPOSE_PROJECT_NAME}
