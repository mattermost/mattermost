version: '2.4'
services:
  postgres:
    image: "postgres:10"
    restart: always
    environment:
      POSTGRES_USER: mmuser
      POSTGRES_PASSWORD: mostest
      POSTGRES_DB: mattermost_test
    command: postgres -c 'config_file=/etc/postgresql/postgresql.conf'
    volumes:
     - "./docker/postgres.conf:/etc/postgresql/postgresql.conf"
    healthcheck:
      test: [ "CMD", "pg_isready", "-h", "localhost" ]
      interval: 5s
      timeout: 10s
      retries: 3
    networks:
      default:
        aliases:
          - postgres
#  ## START SAME AS MYSQL
#  minio:
#    extends:
#      file: gitlab-dc.common.yml
#      service: minio
#    networks:
#      default:
#        aliases:
#          - minio
#  openldap:
#    extends:
#      file: gitlab-dc.common.yml
#      service: openldap
#    networks:
#      default:
#        aliases:
#          - openldap
#  ## END SAME AS MYSQL

  start_dependencies:
    image: mattermost/mattermost-wait-for-dep:latest
    depends_on:
      - postgres
#      - minio
#      - openldap
#    command: postgres:5432 minio:9000 openldap:389
    command: postgres:5432
    networks:
      default:

networks:
  default:
    external:
      name: ${COMPOSE_PROJECT_NAME}
