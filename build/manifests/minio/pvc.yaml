apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio
  labels:
    com.mattermost/component: minio
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
