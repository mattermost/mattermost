import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore — generated by `docusaurus gen-api-docs`. The default export
// is the apisidebar items array (not a full SidebarsConfig object).
import apiReference from '../../api/reference/sidebar';

// API tab sidebar.
//   "Overview" → hand-authored intro page (api/index.mdx)
//   "Examples" → curl quick-start (api/examples.mdx)
//   "Reference" → 38 OpenAPI tag groups (~550 endpoints) generated by
//                 docusaurus-plugin-openapi-docs from
//                 openapi/mattermost-openapi-v4.yaml.
//
// See docs/site/README.md for local dev setup (includes regenerating this).

const sidebars: SidebarsConfig = {
  api: [
    {type: 'doc', id: 'index', label: 'Overview'},
    {type: 'doc', id: 'examples', label: 'Examples'},
    {
      type: 'category',
      label: 'Reference',
      collapsed: false,
      items: apiReference as any[],
    },
  ],
};

export default sidebars;
