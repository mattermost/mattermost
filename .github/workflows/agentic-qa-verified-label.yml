---
name: "QA/Verified"

on:
  pull_request:
    types: [labeled]

permissions: {}

jobs:
  verify-qa:
    permissions:
      contents: read
      pull-requests: read
      statuses: write
    if: github.event.label.name == 'QA/Verified'
    runs-on: ubuntu-24.04
    steps:
      - name: Check write permission
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PERM=$(gh api "repos/${{ github.repository }}/collaborators/${{ github.event.sender.login }}/permission" --jq '.permission')
          if [[ "$PERM" != "admin" && "$PERM" != "write" ]]; then
            echo "User lacks write permission"
            exit 1
          fi

      - name: Override failed QA execution status
        env:
          GH_TOKEN: ${{ github.token }}
          SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          STATE=$(gh api "repos/${{ github.repository }}/commits/${SHA}/status" --jq '.statuses[] | select(.context=="QA/execution") | .state' | head -1)
          if [ "$STATE" = "failure" ] || [ "$STATE" = "error" ]; then
            gh api "repos/${{ github.repository }}/statuses/${SHA}" \
              -f state=success \
              -f context=QA/execution \
              -f description="(verified) Human attestation via QA/Verified label"
          fi
